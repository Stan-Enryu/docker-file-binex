service ${name}
{
        disable = no
        socket_type = stream
        protocol = tcp
        wait = no
        user = ${name}
        type = UNLISTED
        port = ${port}
        bind = 0.0.0.0
        server = /home/${name}/run
        per_source = 10
        rlimit_cpu = 20
}
